# Post-M3 Program: Full Contextual/Core Correspondence

## Evidential position

The only result currently recorded as a project success is the one in `success.md`: the small, lambda-free Core language supports a checked development from the ZFC foundation through construction of the natural numbers.

The completed M3 theorem is narrower. Its contextual terms are only variables, constants, and `raw` terms. It covers a relational/constant fragment, called **M3-R** below. It does not cover ordinary mathematical terms such as:

```text
f(x)
upair(a, b)
bigunion(A)
bigunion(upair(a, b))
nsucc(n)
the x. P(x)
```

PS2 formalizes standard propositional proof search and project-specific certificate plumbing. Its logical results are known results and are not a project success under the rule in `success.md`.

PS2 is frozen as reusable infrastructure. PS3 does not begin over the restricted M3-R language.

The next possible project result is not assumed. It is the hypothesis tested by this program.

